# AGENTS

本仓库当前项目名为 LivePilot，处于截图复盘 Beta 版本开发。目标是给普通主播和直播团队运营人员使用，不暴露复杂技术概念。

## 产品定位

- 核心价值：主播上传抖音后台截图、直播录像或录音后，LivePilot 给出复盘、问题原因、下一场动作、可直接念的话术和风险提醒。
- 产品角色：AI直播运营、AI内容编导、AI实时助理、AI合规助手、AI复盘教练。
- 第一版形态：桌面浏览器 Web 应用，响应式支持手机查看报告，后端 API，异步分析任务。

## 用户体验原则

- 用户少操作，AI 多判断。
- 页面少、选择项少、专业词汇少。
- 每个页面只有一个主要任务。
- 高级设置默认折叠。
- 不复制抖音后台，不做复杂数据罗盘。
- 复盘只突出一个最大问题、两个次要问题、一个保留优势。
- 所有页面文案使用中文。
- 技术错误、模型参数、OCR 置信度等只放在高级设置或错误详情中。

## 固定技术栈

- 前端：Next.js、TypeScript、Tailwind CSS、shadcn/ui 风格组件。
- 后端：FastAPI、Python。
- 基础设施：PostgreSQL、Redis、Celery、S3 兼容存储、FFmpeg、Docker Compose。
- 实时通信：SSE 或 WebSocket。

## 当前开发范围

严格从 Phase 1 开始，不同时铺开全部模块。

Phase 1 必做闭环：

- 登录注册与邀请码。
- 主播档案。
- 模型配置。
- 新建直播复盘。
- 抖音后台截图上传。
- 图片 AI 识别适配层，缺 Key 时使用明确标记的 Mock。
- 数据确认。
- 生成简单复盘。
- 三个核心问题与下一场任务。
- 历史记录。

Phase 1 暂不做：

- 系统音频采集和双音轨识别。
- 自动操作抖音账号。
- 完整视频剪辑器。
- 复杂 BI、能力雷达和重型工作流。

## 目录约定

- `web/`：Next.js 前端。
- `api/`：FastAPI 后端。
- `docs/`：产品、架构、任务、进度和决策文档。
- 旧目录 `backend/`、`admin/`、`miniapp/` 保留，不作为本产品 Beta 的主实现目录。

## 开发要求

- 优先创建和修改实际文件，不只输出方案。
- 每完成一个小任务，尽量运行测试、类型检查、lint、构建，并更新 `docs/TASKS.md` 和 `docs/PROGRESS.md`。
- 仓库可用 Git 时创建提交；若当前目录不是 Git 仓库，先记录限制，再初始化或等待用户确认。
- 缺少模型 Key 时走 Mock 和环境变量，不写死假结果冒充真实模型输出。
- 不删除测试，不关闭类型检查。
- API Key 后端加密或预留加密层，前端脱敏，不写日志，支持测试连接和删除。
- 涉及平台自动操作的能力只做接口预留，不实际执行。
- 时区默认使用 `Asia/Shanghai`。
