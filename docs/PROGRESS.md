# 开发进度

## 2026-06-24：报告页第一屏视觉优化

- 已将报告第一屏改为深色重点卡：
  - 一句话总结；
  - 最大问题；
  - 最大优势；
  - 下一场三个动作。
- 下一场行动卡已补齐序号、执行状态标签、执行时间、可直接说的话和观察指标的视觉层级。
- 报告页主操作按钮、返回修改数据、更换模型和复制话术已统一为当前按钮体系。
- 专业分析仍保持折叠，避免第一屏堆满数据。
- 已完成本次验证：
  - `api/.venv/bin/python -m pytest` 通过，51 个测试；
  - `api/.venv/bin/ruff check .` 通过；
  - `web/npm run typecheck` 通过；
  - `web/npm run lint` 通过；
  - `web/npm run build` 通过。

## 2026-06-24：首页视觉第一轮

- 已将首页主操作区调整为单一主行动“开始新复盘”，并保留“开播准备”为次级行动。
- 已移除首页显眼的实时助手卡，避免当前阶段入口干扰截图复盘 Beta。
- 最近复盘卡已改为结论、最大问题、最大优势和下一场任务的高对比暗色卡片。
- 本周成长任务已改为带序号的轻量任务卡。
- 已完成浏览器抽查：
  - 桌面首页无横向溢出；
  - 手机首页无横向溢出；
  - 手机底部导航正常显示；
  - 未登录/无数据状态下不出现实时助手入口。
- 已完成本次验证：
  - `api/.venv/bin/python -m pytest` 通过，51 个测试；
  - `api/.venv/bin/ruff check .` 通过；
  - `web/npm run typecheck` 通过；
  - `web/npm run lint` 通过；
  - `web/npm run build` 通过。

## 2026-06-24：深色视觉系统第一阶段

- 已建立偏直播生态氛围但不冒充抖音官方的深色设计 Token：
  - 深黑蓝背景；
  - 青蓝主强调色；
  - 玫红辅助强调色；
  - 成功、警告、错误和模拟态色彩基线。
- 已改造全局背景、输入框、浏览器自动填充、焦点态、常见浅色 Tailwind utility 兼容层和打印样式。
- 已改造共享组件：
  - `PageTitle`；
  - `Card`；
  - `PrimaryButton`；
  - `SecondaryButton`；
  - `StatusMessage`。
- 已改造主导航为深色玻璃侧栏和移动端底部导航，并保留“独立工具，与抖音官方无隶属或授权关系”的产品声明。
- 已改造登录页为深色品牌入口。
- 已完成浏览器抽查：
  - 登录页 DOM 无重复卡片和横向溢出；
  - 1440 桌面基线：侧栏显示、底部导航隐藏、无 `.bg-white` 残留；
  - 手机基线：侧栏隐藏、底部四项导航显示、无横向溢出；
  - 浏览器视口已恢复默认。
- 已完成本次验证：
  - `api/.venv/bin/python -m pytest` 通过，51 个测试；
  - `api/.venv/bin/ruff check .` 通过；
  - `web/npm run typecheck` 通过；
  - `web/npm run lint` 通过；
  - `web/npm run build` 通过。

## 2026-06-24：报告生成防重复与流程提示补齐

- 数据确认页已增加前端生成锁，避免用户极快连点导致重复提交报告生成请求。
- 报告生成状态补齐“正在匹配规则与特别提示”，与实际报告上下文构建流程一致。
- 已复核直播复盘列表：当前已支持按主播、抖音账号、状态、开始日期和结束日期筛选。
- 已完成本次验证：
  - `api/.venv/bin/python -m pytest` 通过，51 个测试；
  - `api/.venv/bin/ruff check .` 通过；
  - `web/npm run typecheck` 通过；
  - `web/npm run lint` 通过；
  - `web/npm run build` 通过。

## 2026-06-24：开播方案编辑保存闭环

- 已新增 `PATCH /api/prepare-plans/{plan_id}`，支持保存用户修改后的开播方案内容。
- 开播准备页已将 AI 生成结果改为可编辑文本，支持修改首选主题、标题、开场、互动节点、关注引导、提纲、风险提醒和目标指标后保存。
- 历史方案重新打开后会恢复可编辑草稿；保存后刷新仍保留修改内容。
- 单块按钮文案已改为“按当前信息换一版”，避免误导为只替换局部内容。
- 已新增回归测试，覆盖开播方案编辑后的持久化。
- 已完成本次验证：
  - `api/.venv/bin/python -m pytest` 通过，51 个测试；
  - `api/.venv/bin/ruff check .` 通过；
  - `web/npm run typecheck` 通过；
  - `web/npm run lint` 通过；
  - `web/npm run build` 通过。

## 2026-06-24：平台账号编辑与绑定权限补齐

- 平台账号详情页已支持编辑抖音昵称、抖音号、账号类型、粉丝区间、备注和绑定主播。
- 已支持在平台账号详情中解除主播绑定；解除绑定不会删除已有复盘和报告。
- 后端已补齐主播绑定归属校验，防止把账号绑定到其他用户的主播档案。
- 已新增回归测试，覆盖平台账号解绑和跨用户主播绑定拒绝。
- 已完成本次验证：
  - `api/.venv/bin/python -m pytest` 通过，50 个测试；
  - `api/.venv/bin/ruff check .` 通过；
  - `web/npm run typecheck` 通过；
  - `web/npm run lint` 通过；
  - `web/npm run build` 通过。

## 2026-06-24：反馈上下文追踪补齐

- 已为 `user_feedback` 补充主播、抖音账号、报告版本、模型名称和规则快照字段。
- 报告页提交反馈时，后端会根据直播场次或报告自动关联当前报告版本，不再只保存孤立反馈文本。
- “我的”页反馈列表已显示直播、报告版本、模型和规则依据数量；管理员视图额外显示主播、平台账号和报告 ID。
- 已新增回归测试，覆盖反馈关联报告版本和上下文。
- 已完成本次后端验证：
  - `api/.venv/bin/python -m pytest` 通过，49 个测试；
  - `api/.venv/bin/ruff check .` 通过。

## 2026-06-23

- 已检查仓库：当前目录不是 Git 仓库，无法直接创建提交。
- 已确认旧目录 `backend/`、`admin/`、`miniapp/` 属于历史项目，本 Beta 采用新增 `api/` 和 `web/` 目录。
- 已将 `AGENTS.md` 切换为 AI直播运营助手项目说明。
- 已新增产品、架构、任务、进度、决策文档。
- 已新增 `api/` FastAPI 后端，包含登录注册、邀请码、主播档案、模型配置、直播复盘上传、Mock 图片识别、数据确认、报告生成、首页聚合。
- 已新增 `web/` Next.js 前端，包含登录、首页、主播档案、开播准备、直播复盘、数据确认、直播报告、实时助手、我的、模型设置。
- 已升级前端到 Next.js `16.2.9`，避开安装过程中提示的旧版本安全漏洞。
- 已完成验证：
  - `api/.venv/bin/python -m pytest` 通过；
  - `api/.venv/bin/ruff check .` 通过；
  - `web/npm run typecheck` 通过；
  - `web/npm run lint` 通过；
  - `web/npm run build` 通过。

## 2026-06-23：注册登录阻断修复

- 已确认 FastAPI 启动入口为 `api/app/main.py`，本地运行端口为 `127.0.0.1:8000`。
- 已确认前端开发服务运行在 `127.0.0.1:3000`。
- 已将前端 API Base 统一为 `NEXT_PUBLIC_API_BASE_URL`，默认值为 `http://127.0.0.1:8000`。
- 已新增 `web/.env.example` 和 `web/.env.local`。
- 已将 FastAPI CORS 改为通过 `CORS_ORIGINS` 环境变量管理，默认允许：
  - `http://127.0.0.1:3000`
  - `http://localhost:3000`
- 已修复注册 500：移除 `passlib[bcrypt]` 运行依赖，改用标准库 PBKDF2 密码哈希，避免 `bcrypt 5.0.0` 兼容问题。
- 已改进前端网络错误提示：网络连接失败显示“暂时无法连接服务，请确认后台服务已经启动后重试。”
- 已改进业务错误提示：邀请码错误显示“邀请码无效”，并补充昵称、密码校验中文提示。
- 已在注册页增加开发环境连接状态检查，显示当前后台连接状态和 API Base。
- 已通过 curl 验证：
  - `GET /api/health` 通过；
  - `POST /api/auth/register` 使用 `BETA2026` 通过；
  - `POST /api/auth/login` 通过；
  - CORS 预检允许 `http://127.0.0.1:3000`。
- 已通过浏览器真实验证：
  - 使用 `BETA2026` 注册成功并跳转首页；
  - 退出后登录成功；
  - 错误邀请码显示中文错误“邀请码无效”。
- 已完成本次验证：
  - `api/.venv/bin/python -m pytest` 通过，3 个测试；
  - `api/.venv/bin/ruff check .` 通过；
  - `web/npm run typecheck` 通过；
  - `web/npm run lint` 通过；
  - `web/npm run build` 通过。

## 当前限制

- 缺少真实模型 API Key，Phase 1 使用明确标记为 Mock 的 AI 适配层。
- 根目录初始不是 Git 仓库，本次会初始化 Git 后提交 Beta 骨架。

## 2026-06-23：MVP 范围收缩

- 当前优先目标调整为“抖音后台截图复盘 MVP”。
- 暂停继续开发和前台入口：
  - 音视频上传；
  - FFmpeg；
  - ASR 语音转写；
  - 说话人识别；
  - 连麦分析；
  - 实时助手；
  - 悬浮提词；
  - 高光剪辑；
  - 动态背景；
  - 自动场控。
- 已完成的基础代码不删除，但不作为当前用户主流程。
- 当前核心闭环调整为：创建主播、创建直播复盘、上传多张抖音后台截图、AI 自动识别截图类型和数据、用户确认数据、选择报告模式、生成直播复盘、输出三个核心问题、生成下一场执行方案、保存历史记录、后续场次简单对比。

## 2026-06-23：截图复盘 MVP 入口与报告调整

- 已将主导航收缩为：首页、开播准备、直播复盘、我的。
- 首页实时助手改为“后续开放”，不再作为当前主流程入口。
- 直播复盘页当前只开放多张抖音后台截图上传，移除音视频上传入口。
- 截图上传接口增加图片类型校验，并使用安全文件名保存。
- Mock 图片识别结果新增截图类型自动判断，包含直播概览、流量趋势、用户画像、互动数据、粉丝数据、流量来源等类型。
- 数据确认页展示 AI 自动识别的截图类型，并允许用户修改关键指标。
- 报告方式文案调整为：直接告诉我怎么改、给我专业分析、两种都要。
- 报告第一屏调整为：一句话总结、本场最大问题、本场最大优势、下一场三个动作。
- 三个核心问题改为展示：数据证据、可能原因、下一场怎么改、推荐话术、验证指标。
- 报告新增下一场方案：推荐主题、5 条标题、开场 3 分钟、3 个互动节点、2 个关注引导、1 条新流量承接话术、下一场三个目标。
- 模型设置当前只保留文字分析和图片识别两类用途。
- 已通过真实 API 验证：
  - 使用 `BETA2026` 注册并登录；
  - 创建主播；
  - 创建直播复盘；
  - 一次上传 2 张截图；
  - 自动识别截图类型；
  - 确认关键数据；
  - 生成综合报告；
  - 首页读取最近一场复盘和成长任务成功。
- 已通过浏览器验证：
  - 首页主导航只显示四项；
  - 实时助手显示“后续开放”；
  - 直播复盘页只显示多张截图上传；
  - 报告页第一屏、三个核心问题和下一场方案展示符合 MVP 范围。
- 已完成验证：
  - `api/.venv/bin/python -m pytest` 通过，13 个测试；
  - `api/.venv/bin/ruff check .` 通过；
  - `web/npm run typecheck` 通过；
  - `web/npm run lint` 通过；
  - `web/npm run build` 通过。

## 2026-06-23：真实视觉模型适配层

- 已新增 OpenAI 兼容视觉模型适配器，不在业务代码中绑定具体厂商 SDK。
- 已保留 Mock 视觉模型作为兜底和测试实现，Mock 结果继续明确标记 `source: mock`。
- 已扩展模型配置：
  - 配置名称；
  - API Base；
  - API Key 加密保存；
  - 模型名称；
  - 默认模型；
  - 请求超时时间；
  - 最大重试次数；
  - Mock 模式。
- 已新增模型调用记录表，保存用户、任务、模型配置、模型名、耗时、成功状态、用量和错误类型，不保存完整 API Key。
- 截图识别结果已拆为两类结构化数据：
  - 每张截图的类型、判断理由、置信度、人工确认状态和用户修正类型；
  - 每个指标的原始值、标准化值、单位、来源截图、来源类型、原始文字、置信度、人工确认字段、最终值和识别时间。
- 已实现基础数值标准化：
  - 万、千；
  - 秒、分钟、小时；
  - 百分比；
  - 金额；
  - 小数。
- 已实现重复指标冲突检测，冲突指标不会自动进入确认页的兼容旧字段。
- 当前上传截图接口会优先使用默认图片识别模型；未配置可用真实模型时走 Mock 兜底。
- 已通过真实 API 验证：
  - 使用 `BETA2026` 注册；
  - 创建默认 Mock 图片识别模型配置；
  - 创建主播和直播复盘；
  - 一次上传 2 张截图；
  - 返回 2 条截图分类和 9 条结构化指标字段；
  - 指标字段包含来源截图 ID；
  - `/api/live-sessions/{id}/metrics` 兼容旧数据确认页读取。

## 2026-06-23：数据确认页结构化升级

- 已新增结构化指标确认接口：
  - `PUT /api/live-sessions/{session_id}/recognized-fields`
- 数据确认页改为按分组展示：
  - 核心数据；
  - 流量；
  - 停留；
  - 互动；
  - 关注；
  - 用户画像；
  - 成交；
  - 合规。
- 低置信度数据会用黄色提示，冲突数据会用红色提示。
- 用户可以修改识别值、单位，删除错误字段，或手动新增常用缺失指标。
- 确认后的字段会写回 `recognized_metric_fields`，并标记为人工确认，后续重新识别不会覆盖这些字段。

## 2026-06-23：模型连接真实验证

- 已检查当前默认模型配置，图片识别和文字分析均为自定义 NewAPI 配置，Key 仅脱敏显示。
- 已确认 Base URL 为 OpenAI 兼容路径 `/v1`。
- 已调用 `/v1/models`，当前渠道只返回 `glm-5.1`。
- 已将默认模型名从不可用的 `gpt-4o-mini` 调整为 `glm-5.1`。
- 文本模型真实调用成功，提示“请返回 OK”得到 `OK`。
- 图片模型真实调用失败，接口返回 `glm-5.1` 不是多模态模型。
- 已将前端“测试连接”对应的后端接口改为真实模型调用，不再只是格式检查。
- 此次验证时截图分析流程曾使用 Mock 兜底；后续策略已调整为“显式 Mock 演示”和“正式手动录入”分离。
- 已检查仓库文本、前端代码、环境样例和本项目日志候选文件，未发现 API Key 片段。

## 2026-06-23：真实文字报告与模型策略调整

- 已将文字分析模型和图片识别模型完全拆开：
  - 默认文字模型保留 `glm-5.1`；
  - 图片模型不再默认使用 `glm-5.1`；
  - 图片模型当前状态为未配置。
- 截图上传策略调整：
  - 没有默认图片模型时返回手动录入提示；
  - 显式选择 Mock 图片模型时才进入演示模式；
  - Mock 提示文案为“当前使用模拟识别结果，仅用于功能演示。”
- 已新增基础规则诊断：
  - 最高在线与平均在线差距较大；
  - 累计观看较高但平均在线较低；
  - 平均停留偏低；
  - 停留尚可但互动偏低；
  - 互动较高但关注偏低；
  - 关注转化较好；
  - 评论互动较好；
  - 数据不足；
  - 上一场观看对比。
- 报告生成已改为：规则诊断 → 真实文字模型 → 质量检查 → 保存报告。
- 文字模型报告质量校验失败时会自动重新生成一次；仍失败则标记需要人工检查。
- 真实文字模型失败时才回退 Mock 报告，并记录模型调用失败。
- 报告质量检查覆盖：三个动作、三个问题、验证指标、空泛建议、结果保证和是否引用已确认数据。

## 2026-06-24：Beta 首次使用与手动录入优化

- 首页已按用户状态分流：
  - 没有主播时只提示“先创建一个主播档案”，并提供一个主要按钮；
  - 有主播但没有直播记录时提示“上传第一场直播数据”；
  - 有直播记录时展示最近结论、下一场任务和“复盘新一场直播”入口。
- 手动数据确认页已改为正式可用路径：
  - 第一屏只展示直播日期、直播时长、累计观看、最高在线、平均在线、平均停留、新增关注、评论数、点赞数、是否投流、是否违规；
  - 补充指标放入“补充更多数据”折叠区域；
  - 空字段提交为 `null`，不再默认为 0；
  - 前后端支持 `1.2万`、`3分钟`、`45秒` 等常见输入并标准化；
  - 输入错误显示中文提示。
- 生成报告前新增主播补充说明：
  - 本场直播主题；
  - 本场最想解决的问题；
  - 是否投流；
  - 是否连麦；
  - 主播自评；
  - 设备、网络或其他异常。
- 报告上下文已区分后台数据事实、主播补充描述、系统规则诊断和 AI 推断。
- 历史对比上下文已支持上一场和最近三场可比指标，缺少历史时明确提示“当前缺少历史场次，本次主要根据单场数据分析。”
- 报告页第一屏已改为任务式动作卡片，展示时间、动作、可直接说的话和观察指标。
- 已通过 `next.config.ts` 指定 Turbopack root，消除上层 lockfile 引起的 Next.js 构建警告，未修改旧项目文件。

## 2026-06-24：规则与运营提示中心 MVP

- 已新增规则中心后端数据结构：
  - `RuleEntry`；
  - `RuleVersion`；
  - `RuleInterpretation`；
  - `ReportRuleReference`；
  - `ReportTemporaryInstruction`。
- 已新增规则 API：
  - 规则列表、创建、详情；
  - AI 理解和结构化；
  - 人工确认生效；
  - 修改生成新版本；
  - 停用规则。
- 权限边界：
  - 普通用户可以查看公共规则；
  - 普通用户可以创建、确认和停用自己的主播个人提醒；
  - 普通用户不能发布或修改平台官方规则、系统提示；
  - 管理员通过 `ADMIN_PHONES` 环境变量识别。
- 报告生成已接入规则检索：
  - 只使用“已生效”且未过期的规则；
  - 按规则类型、主播类型、直播形式、关键词、生效时间和主播归属筛选；
  - 优先级为平台官方规则、主播个人提醒、系统提示、运营经验；
  - 只把候选相关规则传给文字模型，不发送整个规则库。
- 报告保存：
  - `model_name`；
  - `prompt_version`；
  - `rule_snapshot`；
  - `generated_at`；
  - 报告引用规则版本关系。
- 前端已在“我的”页面增加“规则与提示”入口，新增简单规则页面。
- 数据确认页已增加临时分析提示，可选择保存为该主播长期提醒并进入待确认流程。
- 报告页已展示规则依据、模型名和 Prompt 版本。

## 2026-06-24：Beta 页面完整性补齐

- 首页增加“截图复盘 Beta”标识，并接入后端规则提醒，用户可直接进入规则与提示中心查看。
- 首页规则提醒后端口径已收窄为公共规则和当前用户自己的主播提醒，不会把其他用户的个人提醒计入。
- 主播档案页支持设置当前主播，并可从主播卡片直接进入该主播的历史直播；直播复盘页会读取当前主播偏好和 `streamer_id` 链接参数。
- 开播准备页读取当前主播偏好，生成方案即保存为下一场计划；每个输出区块支持复制和重新生成，整体支持“换一批”。
- 新建直播复盘页补齐基础信息：直播日期、主题、目标、是否投流、是否连麦和主播补充说明；基础信息会先保存，上传截图后仍保留到确认页和报告上下文。
- 报告页已完成首屏重点内容、专业分析折叠、规则依据、复制、打印/保存 PDF、反馈、重新生成和历史版本查看。
- 直播复盘历史已完成按主播筛选、核心指标、报告状态、继续编辑、删除草稿和归档已完成记录。
- 新建复盘时会读取同一主播上一场已出报告的行动计划，提示用户标记“已执行、部分执行、未执行、不适用”并保存备注；用户也可以跳过。
- “我的”页已接入反馈记录，普通用户查看自己的反馈，管理员可查看最近用户反馈，不新增一级后台。
- 账户与隐私闭环已完成：账户信息、修改密码、退出登录、导出个人数据、直播删除/归档和注销申请均有前后端持久化入口。
- 前端 API 层补齐错误兜底：后台未启动、登录过期、无权限、服务异常、上传失败和非 JSON 响应都会显示中文提示；登录过期会清理本地登录态并返回登录页。
- 已用 Playwright 在生产模式前端验证 1440、1024 和 390 宽度关键页面：
  - 1024 宽度开播准备页侧边导航和空状态正常；
  - 390 宽度直播复盘页底部导航、空状态和主按钮正常；
  - 390 宽度我的页面卡片纵向排列、反馈、账户和隐私入口正常。
- 模型设置页重构为两张简单卡片：
  - 文字分析模型：显示当前模型、状态、测试连接和更换入口；
  - 图片识别模型：显示未配置状态、添加模型和 Mock 演示开关。
- 模型配置表单保留平台模型、自有模型、Mock 演示三种路径，高级超时和重试设置默认折叠。
- 已保存模型支持真实测试连接、脱敏展示和删除，不向前端展示完整 API Key。
- 新增 404 页面和页面异常兜底页，普通用户看到中文说明和明确下一步，不显示异常堆栈。

## 2026-06-24：平台账号与抖音连接预留

- 已将系统用户、主播档案和平台账号拆分建模，新增：
  - `PlatformAccount`；
  - `AnchorPlatformAccount`；
  - `UserPlatformAccount`；
  - `PlatformAuthorization`；
  - `PlatformSyncJob`；
  - `PlatformDataSnapshot`。
- 已为旧直播场次增加兼容字段：
  - `platform_account_id` 可为空；
  - `platform` 默认 `douyin`；
  - `data_source` 默认 `manual_input`。
- 已新增平台账号 API：
  - 手动记录抖音账号；
  - 查看账号列表和详情；
  - 绑定主播；
  - 设为主要账号；
  - 成员关系；
  - 归档；
  - 同步任务 unsupported 返回。
- 已新增 Douyin OAuth Provider 预留，未配置 `DOUYIN_CLIENT_KEY`、`DOUYIN_CLIENT_SECRET`、`DOUYIN_REDIRECT_URI` 时显示“抖音官方授权能力尚未配置”。
- 已在“我的”页面增加“平台账号”入口，并新增平台账号页面。
- 已在创建直播复盘时支持选择主播绑定的抖音账号，只有一个账号时自动带入，没有账号时不阻塞截图复盘。
- 已补齐首页的当前主播和当前抖音账号切换，最近复盘、任务和趋势按当前上下文读取，避免多账号数据混淆。
- 已补齐平台账号详情页：
  - 查看官方能力状态；
  - 邀请已注册成员并设置角色；
  - 显示成员列表；
  - 创建并查看 `unsupported` 同步记录；
  - 展示数据快照空状态。
- 已完成浏览器端验证：
  - 新用户注册；
  - 创建主播；
  - 手动记录抖音账号并绑定主播；
  - 邀请账号成员；
  - 查看同步暂不支持记录；
  - 首页显示当前主播和当前抖音账号；
  - 创建复盘时自动关联主要抖音账号，数据库确认 `platform_account_id` 已保存。
- 已新增 `docs/DOUYIN_INTEGRATION.md`，明确官方接入流程、安全边界和当前能力限制。

## 2026-06-24：Beta功能矩阵审计与第一批缺口修复

- 已在 `docs/TASKS.md` 新增“Beta功能完成矩阵”，按前端页面、后端接口、持久化、权限、状态、测试和浏览器验证逐项标注，不再仅沿用历史完成勾选。
- 已补齐开播准备与复盘之间的真实关联：
  - `PreparationPlan` 增加平台账号、直播形式、连麦、转化、使用状态和使用时间；
  - `LiveSession` 增加 `preparation_plan_id`；
  - 创建复盘时可关联开播方案，并自动标记方案已使用；
  - 开播准备页支持选择抖音账号、直播形式、是否连麦、是否带货或服务转化；
  - 开播方案增加下一场目标指标。
- 已补齐平台账号成员管理：
  - 后端支持修改成员角色；
  - 后端支持移除成员；
  - 至少保留一名负责人；
  - 前端详情页可直接修改角色和移除成员。
- 已补齐模型设置基础管理能力：
  - 后端支持编辑配置、启停字段、切换默认模型；
  - 后端记录测试状态、最近测试时间和最近错误；
  - 删除默认模型前要求先选择替代默认模型；
  - 前端支持编辑、设为默认、显示测试状态。

## 2026-06-24：截图上传体验与后端文件记录补齐

- 已为截图上传增加后端大小限制、格式校验、截图顺序字段、截图列表、排序和删除接口。
- 已修正补传截图的识别范围：重新识别当前直播场次下全部有效截图，避免旧截图数据被新批次覆盖。
- 已在复盘页增加多图预览、单张错误提示、删除、上移/下移、上传进度和前端格式/大小校验。
- 已补充自动化测试覆盖截图上传、列表、排序、删除和错误格式拒绝。
- 已完成校验：`pytest` 44 个测试通过、`ruff` 通过、前端 `typecheck`/`lint`/`build` 通过。

## 2026-06-24：历史复盘筛选与账号隔离补齐

- 直播复盘历史接口已支持按主播、抖音账号、报告状态、直播日期范围筛选。
- 历史摘要已返回直播日期和关联抖音账号，页面列表不再只显示创建时间。
- 复盘页历史记录已增加抖音账号、状态、开始日期、结束日期筛选和清空筛选入口。
- 已补充自动化测试覆盖同主播多抖音账号下的历史筛选，避免账号数据混淆。
- 已完成校验：`pytest` 45 个测试通过、`ruff` 通过、前端 `typecheck`/`lint`/`build` 通过。

## 2026-06-24：手动数据录入补充字段增强

- 数据确认页已补充曝光人数、进入人数、曝光进入率、推荐流量占比、粉丝流量占比、分享数、观看关注率、用户画像、商品点击、成交人数、成交金额、退款情况、违规提示内容等常见字段。
- 已支持自定义指标名称、单位和值，保存到 `additional_metrics`，供后续报告上下文使用。
- 已调整前端校验：文本类指标不再强制输入数字，自定义指标要求填写指标名称。
- 已补充自动化测试覆盖额外手动指标持久化和常见单位标准化。
- 已完成校验：`pytest` 46 个测试通过、`ruff` 通过、前端 `typecheck`/`lint`/`build` 通过。

## 2026-06-24：报告历史版本查看补齐

- 新增报告历史版本详情接口，可按版本 ID 读取当时保存的完整报告 JSON。
- 报告页历史版本列表已支持点击查看旧版本，并显示“正在查看历史版本”的提示。
- 报告页增加“更换模型”入口，避免重新生成前找不到模型设置。
- 已补充自动化测试覆盖旧报告版本读取。
- 已完成校验：`pytest` 46 个测试通过、`ruff` 通过、前端 `typecheck`/`lint`/`build` 通过。

## 2026-06-24：浏览器验收发现问题修复

- 已用浏览器走通注册、创建主播、手动记录抖音账号、创建复盘、手动录入、生成报告和报告页查看。
- 修复手动创建复盘后错误提示“截图识别完成”的文案，改为“复盘草稿已创建”。
- 修复数据确认页把系统核心字段误显示为“手动新增指标”的问题。
- 修复旧版/手动数据进入确认页时核心指标标签显示英文 key 的问题。
- 已完成校验：`pytest` 46 个测试通过、`ruff` 通过、前端 `typecheck`/`lint`/`build` 通过。

## 2026-06-24：首页工作台指标和执行摘要补齐

- 首页 `dashboard` 接口已按当前主播和当前抖音账号上下文返回最近两场关键指标变化。
- 首页已展示最近一场最大优势、最近指标变化和上一场行动计划执行摘要。
- 首页聚合已默认排除归档主播和归档直播，避免旧数据干扰当前工作台。
- 已新增测试覆盖同一账号上下文下的首页指标变化和执行摘要。
- 已完成校验：`pytest` 47 个测试通过、`ruff` 通过、前端 `typecheck`/`lint`/`build` 通过。

## 2026-06-24：主播默认选择与归档恢复补齐

- 用户表新增 `default_streamer_id`，用于持久化默认主播，不再只依赖浏览器本地存储。
- 新增设为当前主播接口和恢复归档主播接口。
- 新建第一个主播时会自动成为默认主播；归档或删除默认主播时会清空默认值。
- 首页、开播准备、直播复盘都会优先使用后端默认主播。
- 主播管理页已支持恢复归档主播。
- 已新增测试覆盖默认主播设置、归档后恢复和再次设为默认。
- 已完成校验：`pytest` 48 个测试通过、`ruff` 通过、前端 `typecheck`/`lint`/`build` 通过。

## 2026-06-24：Beta 完成清单与基础交互补全

- 已在 `docs/TASKS.md` 新增“截图复盘 Beta 完成清单”，按登录、首页、主播、开播准备、复盘、报告、历史、规则、模型、反馈、账户隐私、错误状态、响应式和打印导出逐项验收。
- 主播档案补全：
  - 当前平均在线区间；
  - 常用直播时间；
  - 当前最想提升的问题；
  - 简单主播介绍；
  - 编辑主播；
  - 有直播记录时归档，不直接永久删除。
- 直播复盘历史列表补全：
  - 按主播筛选；
  - 展示直播时长、最高在线、平均在线、新增关注和最大问题；
  - 未完成复盘可删除；
  - 已完成复盘可归档。
- 复盘创建补全：
  - 支持有截图时上传识别；
  - 无截图或无图片模型时可直接进入手动录入。
- 下一场行动补全：
  - 每条行动可标记“已执行、部分执行、未执行、不适用”；
  - 支持填写一句备注；
  - 状态保存到 `growth_tasks`，刷新后仍存在。
- 反馈与账户隐私补全：
  - 报告页和“我的”页可提交反馈；
  - 新增反馈数据表和 API；
  - “我的”页支持查看账户信息、修改密码、退出登录、导出个人数据、提交注销申请。
- 报告页补全：
  - 复制下一场任务；
  - 复制单条话术；
  - 打印/浏览器保存 PDF；
  - 返回修改数据；
  - 移除无效“加入成长任务”按钮。
- 已新增后端测试覆盖主播归档、行动状态保存、反馈和注销申请。

## 2026-06-24：开播准备保存闭环

- 开播准备页已从纯前端草稿改为后端保存：
  - 选择主播；
  - 输入下一场主题；
  - 填写预计时长；
  - 选择提升目标；
  - 填写本次特别注意事项。
- 后端新增 `PreparationPlan` 数据表和 `/api/prepare-plans` 接口。
- 生成结果包含：
  - 首选主题；
  - 2 个备选主题；
  - 5 条标题；
  - 开场 3 分钟；
  - 3 个互动节点；
  - 2 个关注引导；
  - 新流量承接话术；
  - 简版直播提纲；
  - 合规注意事项；
  - 当前匹配规则快照。
- 前端支持复制单项内容、换一批、查看最近保存方案，刷新后结果仍存在。
- 已新增后端测试覆盖开播计划保存。

## 2026-06-24：报告版本与重新生成

- 新增 `ReviewReportVersion` 数据表。
- 每次生成或重新生成报告都会保存一个版本：
  - 版本号；
  - 报告模式；
  - 模型名称；
  - 规则快照；
  - 数据快照；
  - 是否当前版本；
  - 生成时间。
- 报告接口默认返回最新版本，并附带当前版本号、报告 ID 和报告模式。
- 新增报告版本列表接口。
- 报告页支持：
  - 更换报告模式；
  - 重新生成；
  - 查看历史版本；
  - 保留旧版本记录。
- 已新增后端测试覆盖同一场报告重复生成后的版本递增和当前版本标记。

## 2026-06-24：规则中心权限修正

- 已修正规则中心权限：
  - 普通用户只能创建和管理自己的“主播个人提醒”；
  - 平台官方规则、运营经验、系统提示均视为公共规则，只允许管理员创建和修改；
  - 普通用户仍可查看已生效公共规则。
- 前端普通用户添加规则时只显示“主播个人提醒”。
- 已新增测试覆盖普通用户不能创建公共运营经验。

## 2026-06-24：规则中心编辑与版本查看

- 新增规则版本列表接口。
- 规则详情页支持：
  - 编辑规则标题；
  - 编辑规则内容；
  - 保存后重新 AI 理解；
  - 查看历史版本。
- 已新增测试覆盖规则更新后版本列表递增。

## 2026-06-23：Phase 2 音视频上传

- 已完成 Phase 2 第一项“录像和录音上传”。
- 后端新增本地存储适配层 `api/app/storage.py`，支持安全文件名、类型判断、大小限制和本地保存。
- 后端支持 MP4、MOV、MKV、FLV、MP3、WAV、M4A、AAC。
- 后端新增 `AnalysisJob` 任务记录，音视频上传后创建 `media_content_analysis` 任务，状态为 `waiting`。
- 后端媒体上传接口：
  - `POST /api/live-sessions/{session_id}/media`
  - `GET /api/live-sessions/{session_id}/media`
  - `GET /api/analysis-jobs/{job_id}`
  - `POST /api/analysis-jobs/{job_id}/retry`
- 前端复盘页已支持选择直播录像或录音、显示上传进度、展示处理状态和失败重试入口。
- 已通过真实 API 验证：
  - 注册新账号；
  - 创建主播；
  - 创建直播记录；
  - 上传测试 MP3；
  - 返回安全重命名文件、`uploaded` 文件状态和 `waiting` 处理任务；
  - 媒体列表可查询到对应文件与任务。
- 已完成验证：
  - `api/.venv/bin/python -m pytest` 通过，8 个测试；
  - `api/.venv/bin/ruff check .` 通过；
  - `web/npm run typecheck` 通过；
  - `web/npm run lint` 通过；
  - `web/npm run build` 通过。

## 2026-06-23：Phase 2 FFmpeg 处理入口

- 已新增 `api/app/media_processing.py`，包含 FFmpeg/FFprobe 可用性检查、视频提取音频、音频标准化和按时间偏移切片流程。
- 已新增 `AudioChunk` 数据模型，用于保存切片编号、开始时间、结束时间和切片文件路径。
- 已新增任务处理接口：
  - `POST /api/analysis-jobs/{job_id}/process-media`
  - `GET /api/analysis-jobs/{job_id}/audio-chunks`
- 已实现任务状态推进：
  - `waiting`
  - `processing`
  - `completed`
  - `failed`
- 当前本机未安装 `ffmpeg`/`ffprobe`，已验证不可用分支：
  - 任务状态变为 `failed`；
  - 返回中文错误“音视频处理工具不可用，请安装 FFmpeg 后重试。”；
  - 返回安装说明“macOS 可执行：brew install ffmpeg”；
  - 不向普通用户暴露底层异常堆栈。
- 已完成本次后端验证：
  - `api/.venv/bin/python -m pytest` 通过，10 个测试；
  - `api/.venv/bin/ruff check .` 通过。

## 2026-06-23：Phase 2 ASR 适配层

- 已新增 Mock ASR 适配层，结果明确标记 `source: mock`。
- 已新增 `TranscriptSegment` 数据模型，保存：
  - 开始时间；
  - 结束时间；
  - 文本；
  - 说话人；
  - 置信度；
  - 来源文件；
  - 分片编号。
- 已新增转写接口：
  - `POST /api/analysis-jobs/{job_id}/transcribe`
  - `GET /api/analysis-jobs/{job_id}/transcript-segments`
- 已实现转写幂等：已有片段时直接返回，不重复生成。
- 当前无法可靠区分主播和连麦人，统一标记为“未知”，不伪造说话人身份。
- 已通过真实 API 验证：
  - 创建上传任务；
  - 准备测试音频切片；
  - 调用转写接口；
  - 返回 2 条带时间戳片段；
  - 查询转写片段列表成功。

## 2026-06-23：Phase 2 内容分析

- 已新增内容分析适配层，基于转写片段生成结构化内容分析。
- 已新增 `ContentAnalysis` 数据模型，用于持久化内容分析 JSON。
- 已新增内容分析接口：
  - `POST /api/analysis-jobs/{job_id}/content-analysis`
  - `GET /api/analysis-jobs/{job_id}/content-analysis`
- 已覆盖识别项：
  - 开场结构；
  - 主题段落；
  - 内容重复；
  - 长时间单向输出；
  - 互动问题；
  - 关注引导；
  - 新用户承接；
  - 冷场片段；
  - 连麦片段；
  - 高价值观点；
  - 高光片段；
  - 风险表达初筛。
- 已通过真实 API 验证：
  - 创建上传任务；
  - 准备测试音频切片；
  - 调用 Mock ASR 转写；
  - 调用内容分析；
  - 返回 `source: mock`、开场结构、核心问题、互动数量、高光数量和风险数量。

## 2026-06-23：Phase 2 话术风险

- 已新增 `SpeechRisk` 数据模型，用于独立持久化风险表达。
- 已新增风险识别适配层，覆盖：
  - 极端判断、婚恋煽动、男女对立、群体攻击、人身攻击；
  - 医疗或心理诊断、法律定性、政治敏感、未成年人；
  - 色情低俗、暴力和自伤、隐私泄露、诱导打赏、站外引流、虚假宣传、绝对化承诺。
- 已新增接口：
  - `POST /api/analysis-jobs/{job_id}/speech-risks`
  - `GET /api/analysis-jobs/{job_id}/speech-risks`
- 每条风险输出：
  - 时间点；
  - 原始内容；
  - 风险类型；
  - 风险等级；
  - 简短原因；
  - 推荐改写；
  - 是否需要人工确认。
- 已通过真实 API 验证：包含“百分百保你”的转写片段会生成“绝对化承诺”风险，且包含推荐改写和人工确认标记。
