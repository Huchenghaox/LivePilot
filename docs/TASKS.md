# 任务拆分

## 当前生产可用化与多用户安全

- [x] 强化认证与用户边界：删除用户不能登录或继续使用旧 Token。
- [x] 增加服务就绪检查：`/api/ready` 和根路径 `/ready` 检查数据库与上传目录。
- [x] 平台账号去重修复：手动账号重复时不再把陌生用户自动加入成员。
- [x] 平台账号按主播筛选增加后端归属校验，拒绝使用其他用户主播 ID 查询。
- [x] 首页工作台增加真实 onboarding 指标：主播数、平台账号数、方案数、模型状态。
- [x] 注册页增加确认密码、密码显示切换、服务条款/隐私确认和重复提交保护。
- [x] 新增服务条款与隐私基础文档和前端入口。
- [x] 生产配置增加 `APP_ENV` 校验：生产环境拒绝默认 JWT Secret 和空 CORS。
- [x] 增加 Dockerfile、Compose、健康检查和 GitHub Actions CI 草案。
- [x] 增加首次生产部署、华为服务器、Cloudflare Tunnel、域名绑定、迁移路线图和发布清单文档。
- [x] 依赖安全审计：已升级可安全处理的 ESLint/PostCSS 低风险依赖。
- [ ] 本机 Docker Compose 命令不可用，待在装有 Docker Compose 的机器或华为服务器上验证 `docker compose config/build/up`。
- [ ] 正式数据库迁移机制仍待实现，当前仅记录 `docs/DATABASE_MIGRATIONS.md` 方案。
- [ ] 邮箱验证、密码找回和分布式限流仍待后续实现。
- [ ] 最终 LICENSE 仍待产品负责人确认，本轮不创建正式许可证文件。

## 本轮开源与演示准备

- [x] 继续统一剩余页面深色视觉基线：错误页、404、实时助手预览和全局旧控件样式。
- [x] 完成未登录状态桌面和手机浏览器烟测。
- [x] 完成登录态核心数据闭环烟测：注册、主播、平台账号、开播方案、复盘、指标、报告、反馈、历史。
- [x] 新增 README，统一项目名称、定位、本地运行、测试命令、路线图和 Cloudflare 迁移状态。
- [x] 新增贡献指南、公开安全反馈说明、本地开发文档、部署概览和演示指南。
- [x] 新增许可证建议文档，等待产品负责人确认正式许可证。
- [x] 新增依赖安全审计文档，记录 npm audit 的 2 个 low 和 2 个 moderate。
- [ ] 后续补齐正式产品截图或录屏素材，当前 README 仅保留截图占位说明。
- [ ] 后续在确认许可证后创建最终 `LICENSE` 文件。

## Beta功能完成矩阵

> 审计口径：以当前代码、接口、持久化和浏览器验证为准；不是仅依据历史勾选。状态分为：完成、进行中、待补齐、后续开放。

| 功能名称 | 前端页面 | 后端接口 | 数据持久化 | 权限校验 | 空状态 | 加载状态 | 失败状态 | 自动化测试 | 浏览器人工验证 | 当前完成状态 | 剩余问题 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 登录注册与邀请码 | `/login` | `/api/auth/register`, `/api/auth/login`, `/api/me` | users | 是 | 是 | 是 | 是 | 有 | 已验证 | 完成 | 保持回归测试 |
| 首页工作台 | `/` | `/api/dashboard`, `/api/streamers`, `/api/platform-accounts` | live_sessions, growth_tasks | 是 | 是 | 是 | 是 | 有 | 已验证账号上下文和响应式基线 | 完成 | 已补深色主操作区、最近复盘卡、任务卡和实时助手入口收敛；保持回归测试 |
| 主播档案 | `/streamers` | `/api/streamers` | streamers, users.default_streamer_id | 是 | 是 | 是 | 是 | 有 | 部分验证 | 完成 | 已补恢复归档主播、默认主播持久化和主流程默认选择；待最终浏览器巡检 |
| 平台账号 | `/platform-accounts` | `/api/platform-accounts*` | platform_accounts, user_platform_accounts, anchor_platform_accounts | 是 | 是 | 是 | 是 | 有 | 已验证创建/成员/复盘关联 | 完成 | 已补编辑、绑定/解绑主播、成员改角色/移除和后端归属权限校验；待最终浏览器巡检 |
| 开播准备 | `/prepare` | `/api/prepare-plans` | preparation_plans | 是 | 是 | 是 | 是 | 有 | 待复验 | 完成 | 已补生成、编辑保存、复制、历史重开、标记使用、账号和复盘关联，并完成深色开播编导视觉；待最终浏览器巡检 |
| 直播复盘创建 | `/review` | `/api/live-sessions`, `/api/live-sessions/{id}/metrics` | live_sessions, recognized_metrics | 是 | 是 | 是 | 是 | 有 | 已验证基础流程 | 完成 | 已补开播方案关联、日期筛选、状态筛选、账号筛选和深色步骤条；待最终浏览器巡检 |
| 截图上传 | `/review` | `/api/live-sessions/{id}/screenshots` | uploaded_assets, screenshot_recognitions, recognized_metric_fields | 是 | 是 | 是 | 是 | 有 | 待复验 | 进行中 | 已补多图预览、前后端类型/大小校验、上传进度、前端删除/排序、后端截图列表/删除/排序和深色上传状态；待浏览器复验失败重试 |
| 手动数据录入 | `/review/{id}/confirm` | `/api/live-sessions/{id}/recognized-fields`, `/api/live-sessions/{id}/metrics` | recognized_metric_fields, recognized_metrics | 是 | 是 | 是 | 是 | 有 | 已验证主路径 | 进行中 | 已补曝光、进入、流量占比、分享、画像、成交、退款、违规原文、自定义指标和深色确认页；待浏览器复验 |
| 报告生成与版本 | `/review/{id}/confirm`, `/report/{id}` | `/api/live-sessions/{id}/report`, `/report-versions` | review_reports, review_report_versions, growth_tasks | 是 | 是 | 是 | 是 | 有 | 已验证基础报告 | 完成 | 已补生成步骤提示、前端防重复生成锁、历史版本内容查看和更换模型入口；待最终浏览器巡检 |
| 历史复盘与对比 | `/review`, `/report/{id}` | `/api/live-sessions`, report context | live_sessions, recognized_metrics | 是 | 是 | 是 | 是 | 有 | 部分验证 | 进行中 | 已补按主播、抖音账号、日期、状态筛选和账号隔离测试；待补自定义两场对比浏览器复验 |
| 建议执行闭环 | `/report/{id}`, `/review` | `/api/growth-tasks` | growth_tasks | 是 | 是 | 是 | 是 | 有 | 部分验证 | 完成 | 继续浏览器复验下一场读取 |
| 规则与提示中心 | `/rules` | `/api/rules*` | rule_entries, rule_versions, rule_interpretations, report_rule_references | 是 | 是 | 是 | 是 | 有 | 待复验 | 进行中 | 待补更细空状态、公共规则权限浏览器验证 |
| 模型设置 | `/model-settings` | `/api/model-settings*` | model_settings, model_call_logs | 是 | 是 | 基础 | 是 | 有 | 待复验 | 进行中 | 已补编辑、设默认、测试状态；待浏览器复验 |
| 用户反馈 | `/report/{id}`, `/me` | `/api/feedback` | user_feedback | 是 | 是 | 是 | 是 | 有 | 部分验证 | 完成 | 已补反馈关联主播、抖音账号、报告版本、模型名和规则快照；待最终浏览器巡检 |
| 账户与隐私 | `/me` | `/api/account/*` | users 和用户数据表 | 是 | 是 | 是 | 是 | 有 | 部分验证 | 进行中 | 待补上传图片删除、解除平台账号关联、软删除处理视图 |
| 系统错误状态 | `/not-found`, `/error`, API错误统一处理 | 多接口 | 不适用 | 是 | 是 | 是 | 是 | 部分 | 部分验证 | 进行中 | 待补无权限/模型未配置/分析失败专用体验 |
| 响应式与视觉统一 | 全站 | 不适用 | 不适用 | 不适用 | 基础 | 基础 | 基础 | 构建检查 | 已验证桌面/手机基线 | 进行中 | 已完成深色设计 Token、全局背景、导航、按钮、卡片、表单、首页、开播准备、复盘创建/截图上传、数据确认、报告第一屏和打印白底基线；待逐页细化规则/模型视觉 |
| 音视频/实时/高光/自动场控 | 隐藏或后续开放 | 已有部分底层但非当前入口 | 部分 | 部分 | 不适用 | 不适用 | 不适用 | 既有测试 | 不验收 | 后续开放 | 本轮不继续扩展 |

## 截图复盘 Beta 完成清单

> 勾选标准：前端页面存在、后端接口存在、数据可真实保存、刷新后仍存在、错误状态已处理、权限已验证、关键流程有测试、浏览器人工验证通过。

- [x] 登录注册：邀请码注册、登录、退出、登录错误和后台不可用提示。
- [x] 首页：首次使用、无复盘、有报告三种状态，最近结论、下一场任务和规则提醒。
- [x] 主播：新建、编辑、归档/删除、历史直播查看、当前主播切换。
- [x] 开播准备：一步式输入、生成主题/标题/开场/互动/关注/提纲、复制和保存计划。
- [x] 创建复盘：基础信息、上传截图或手动填写两条路径。
- [x] 上传截图：多图上传、图片类型校验、Mock 演示明确标识、无图片模型时手动录入。
- [x] 手动录入：核心字段、补充字段、常见单位、空值 null、中文错误提示。
- [x] 数据确认：分组、修改、删除、新增、冲突提示、返回继续上传。
- [x] 报告生成：三种模式、临时提示、规则注入、真实文字模型、质量检查、下一场方案。
- [x] 报告查看：首屏、专业折叠、规则依据、复制、打印、反馈、重新生成、历史版本。
- [x] 历史记录：按主播筛选、核心指标、状态、继续编辑、删除草稿、归档完成记录。
- [x] 历史对比：上一场和最近三场可比指标进入报告上下文。
- [x] 建议执行：状态、备注、下一场复盘时提示上一场执行情况。
- [x] 规则中心：公共规则、主播提醒、AI理解、确认、编辑、停用、版本和权限。
- [x] 模型设置：文字模型、图片模型、Mock演示、真实测试、脱敏和删除。
- [x] 反馈：报告页和我的页提交，管理员查看。
- [x] 账户隐私：账户信息、改密、退出、导出数据、删除/归档直播、注销申请。
- [x] 错误状态：未启动、登录过期、模型异常、上传失败、数据不足、无权限、404、服务异常。
- [x] 响应式：1440、1280、1024 和手机报告查看。
- [x] 打印导出：报告打印、浏览器保存 PDF、任务和话术复制。

## 平台账号与抖音连接

- [x] 新增 `PlatformAccount`、用户成员关系、主播绑定关系、授权、同步任务和数据快照数据结构。
- [x] 兼容旧直播场次：`platform_account_id` 可为空，旧报告和历史记录继续可访问。
- [x] 支持手动记录抖音账号，并明确显示“已手动记录”，不伪装官方连接。
- [x] 支持一个用户管理多个抖音账号，一个主播绑定多个账号，一个账号多个成员。
- [x] 支持主播主要抖音账号设置。
- [x] 创建直播复盘时选择或自动带入主播绑定的抖音账号。
- [x] 保存直播场次的平台、平台账号和数据来源。
- [x] 预留 Douyin OAuth Provider 接口，未配置时返回清晰中文提示。
- [x] 官方同步能力当前返回 `unsupported`，不伪造直播数据。
- [x] 新增 `docs/DOUYIN_INTEGRATION.md`，记录抖音开放平台接入、安全边界和能力限制。
- [x] 首页当前主播和当前抖音账号切换。
- [x] 平台账号详情页成员邀请和同步记录的完整交互。
- [x] 浏览器端完整验证平台账号创建、绑定、复盘关联。

## Phase 1：基础可用闭环

- [x] 建立 AI直播运营助手项目文档。
- [x] 修复注册登录阻断：API Base 环境变量、CORS 环境化、注册密码哈希、中文错误提示、浏览器真实注册登录验证。
- [x] 初始化 FastAPI 后端工程。
- [x] 初始化 Next.js 前端工程。
- [x] 登录与邀请码注册 API。
- [x] 主播档案 API 与页面。
- [x] 模型配置 API 与页面，支持 Mock、脱敏、测试连接、删除。
- [x] 新建直播复盘与截图上传 API。
- [x] 图片识别适配层，缺 Key 使用明确标记的 Mock。
- [x] 数据确认 API 与页面。
- [x] 简单复盘报告生成 API。
- [x] 三个核心问题、下一场任务、历史记录页面。
- [x] 首页聚合最近一场直播与本周任务。
- [x] Phase 1 测试、类型检查、lint、构建。

## 当前 MVP：抖音后台截图复盘

- [x] 范围收缩：暂停音视频、FFmpeg、ASR、说话人识别、连麦分析、实时助手、高光剪辑、动态背景和自动场控前台入口。
- [x] 主导航只保留：首页、开播准备、直播复盘、我的。
- [x] 直播复盘页只开放多张抖音后台截图上传。
- [x] AI 图片识别结果包含自动截图类型判断。
- [x] 数据确认页允许修改关键指标。
- [x] 报告模式文案调整为：直接告诉我怎么改、给我专业分析、两种都要。
- [x] 报告第一屏只展示一句话总结、最大问题、最大优势、下一场三个动作。
- [x] 三个核心问题包含数据证据、可能原因、下一场怎么改、推荐话术、验证指标。
- [x] 下一场方案包含推荐主题、5 条标题、开场 3 分钟、3 个互动节点、2 个关注引导、1 条新流量承接话术、下一场三个目标。
- [x] 模型配置只保留图片识别和文字分析。
- [x] 完成截图复盘 MVP 浏览器端真实流程验证。

## 当前阶段：真实模型截图复盘 Beta

- [x] 完善统一模型适配层，新增 OpenAI 兼容视觉接口与 Mock 兜底。
- [x] 图片识别模型配置支持配置名称、API Base、API Key、模型名称、启用状态、默认模型、超时、重试。
- [x] API Key 后端加密保存，前端只显示脱敏结果。
- [x] 截图自动分类限制为直播概览、流量趋势、流量来源、用户画像、互动数据、关注数据、违规提示、带货数据、无法识别。
- [x] 视觉模型返回严格结构化 JSON 后再写入数据库。
- [x] 识别字段支持原始值、标准化值、单位、来源截图、置信度、人工确认字段、最终值和识别时间。
- [x] 支持万、千、秒、分钟、小时、百分比、金额等基础标准化。
- [x] 支持重复指标冲突检测，冲突时不自动覆盖。
- [x] 数据确认页按核心数据、流量、停留、互动、关注、用户画像、成交、合规分组展示字段明细。
- [x] 人工确认后形成不可被 AI 覆盖的最终数据版本。
- [x] 模型策略调整：文字模型与图片模型独立，当前图片模型未配置，Mock 只作为显式演示模式。
- [x] 实现规则诊断引擎。
- [x] 实现真实文本模型报告。
- [x] 实现报告质量检查。
- [x] 优化下一场方案，使其结合主播类型和真实诊断。
- [x] 优化首次使用首页：无主播、无直播记录、有记录三种状态分别引导。
- [x] 优化手动数据录入：核心指标第一屏、补充数据折叠、空值保存为 null、常见中文单位标准化。
- [x] 生成报告前支持主播补充说明，并传入文字模型上下文。
- [x] 优化报告生成状态文案和报告第一屏动作卡片。
- [x] 增加基础历史对比上下文：上一场和最近三场可比指标。
- [x] 修复 Next.js workspace root 警告，不修改旧项目文件。
- [x] 增加规则与运营提示中心数据结构：规则、版本、AI理解、报告引用和临时提示。
- [x] 增加规则录入、AI理解、人工确认、停用和列表接口。
- [x] 在“我的”页面增加规则与提示入口和简单规则页面。
- [x] 报告生成时注入相关有效规则和临时分析提示。
- [x] 报告页展示规则依据、模型名和 Prompt 版本。
- [x] 开播准备接入真实保存接口：主播、主题、时长、目标、特别注意、方案结果刷新不丢失。
- [ ] 使用真实抖音后台截图做端到端验证。
- [ ] 建议执行追踪：已执行、部分执行、未执行、不适用和备注。
- [x] 报告版本管理与重新生成。
- [ ] Beta 反馈入口。
- [ ] 真实 GLM-5.1 三组端到端验证。
- [ ] 规则中心浏览器端人工验证：个人提醒、公共规则权限、规则确认和报告引用。

## Phase 2：内容分析

> 当前暂停继续开发。已完成的基础代码保留，不作为截图复盘 MVP 的前台入口。

- [x] 录像和录音上传。
  - [x] 支持 MP4、MOV、MKV、FLV、MP3、WAV、M4A、AAC。
  - [x] 文件类型校验、大小配置、安全文件名。
  - [x] 本地开发存储，并预留 S3 存储适配层边界。
  - [x] 用户权限校验、直播场次归属校验、数据库文件记录。
  - [x] 上传进度、失败重试入口、文件处理状态展示。
  - [x] 上传后创建 `waiting` 处理任务，避免直接阻塞页面。
- [ ] FFmpeg 音频提取。
  - [x] FFmpeg/FFprobe 服务层。
  - [x] 视频提取音频、音频标准化、长音频按时间偏移切片的命令流程。
  - [x] 不一次加载完整长音频到内存。
  - [x] FFmpeg 不可用时返回中文错误和安装说明，不暴露技术堆栈。
  - [x] 任务状态从 `waiting` 推进到 `processing`、`completed` 或 `failed`。
  - [ ] 安装 FFmpeg 后完成真实音视频端到端提取验证。
- [x] 语音转写适配层。
  - [x] Mock ASR 统一接口。
  - [x] 转写片段持久化：开始时间、结束时间、文本、说话人、置信度、来源文件、分片编号。
  - [x] 无法区分说话人时标记为“未知”，不伪造主播或连麦人身份。
  - [x] 转写接口幂等：已有片段时不重复生成。
  - [x] 预留真实 ASR 和说话人分离接口边界。
- [x] 内容分析。
  - [x] 开场结构识别。
  - [x] 主题段落识别。
  - [x] 内容重复和单向输出提示。
  - [x] 互动问题、关注引导、新用户承接识别。
  - [x] 冷场、连麦、高价值观点、高光片段识别。
  - [x] 内容分析结果持久化且接口幂等。
- [x] 话术风险。
  - [x] 覆盖 16 类风险初筛。
  - [x] 每条风险包含时间点、原始内容、风险类型、风险等级、简短原因、推荐改写、是否需要人工确认。
  - [x] 风险结果独立持久化并可查询。
- [ ] 内容型报告。
- [ ] 综合报告。
- [ ] 完整直播稿。

## Phase 3：播前准备

- [ ] 主题生成。
- [ ] 标题生成。
- [ ] 分段直播稿。
- [ ] 合规筛查。
- [ ] 封面生成。
- [ ] 静态背景生成。
- [ ] 开播检查。

## Phase 4：实时助手

- [ ] 浏览器麦克风。
- [ ] 实时转写。
- [ ] 网页提词。
- [ ] 基础语速跟随。
- [ ] 快捷救场。
- [ ] 基础风险提醒。

## Phase 5：高光切片

- [ ] 高光识别。
- [ ] FFmpeg 切片。
- [ ] 自动字幕。
- [ ] 短视频标题。
- [ ] 发布话题。
- [ ] 9:16 导出。
